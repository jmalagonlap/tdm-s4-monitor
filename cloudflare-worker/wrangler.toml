name = "tdm-gps-monitor"
main = "src/index.js"
compatibility_date = "2024-09-23"

# KV Namespace — crear en Cloudflare dashboard y pegar el ID aquí
[[kv_namespaces]]
binding = "GPS_KV"
id = "REEMPLAZAR_CON_KV_NAMESPACE_ID"

# Cron: cada 1 minuto
[triggers]
crons = ["* * * * *"]
